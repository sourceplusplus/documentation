# Library Check

The **Library Check** command allows you to check if a specific library is included in your deployed application. This
command is useful for determining if a specific library is included in your application and if it is, what version is
being used.

## Available Contexts

- Any

## Parameters

- Library Name
    - *The name of the library to check for.*
